gz file command 

curl -o green_tripdata_2019-09.csv.gz https://github.com/DataTalksClub/nyc-tlc-data/releases/download/green/green_tripdata_2019-09.csv.gz

next command 

curl -o taxi_zone_lookup.csv https://s3.amazonaws.com/nyc-tlc/misc/taxi+_zone_lookup.csv           


next 
Create a simple docker file with entrypoint 

FROM python:3.9

#Install pandas
RUN pip install pandas 

#Override this entrypoint
ENTRYPOINT [ "bash" ]


in your terminal now, build an "image" - Docker Image off the Dockerfile 

docker build -t test-image:pandas . # the dot is for docker to build the image


it will download and install pandas 

after building you csan run the image 

docker run -it test-image:pandas

because entry point is bash, you will get a bash here 

remeber to use version control as you go 




docker run -it \
  -e POSTGRES_USER="root" \
  -e POSTGRES_PASSWORD="root" \
  -e POSTGRES_DB="ny_taxi" \
  -v /workspace/DataEngineeringZoomCamp/ny_taxi_postgres_data:/var/lib/postgres/data \
  -p 5432:5432 \
  postgres:13


  pip install pgcli

  query the db 

  install jupyter 

  pd.__version__


wget https://github.com/DataTalksClub/nyc-tlc-data/releases/download/green/green_tripdata_2019-09.csv.gz

wget https://s3.amazonaws.com/nyc-tlc/misc/taxi+_zone_lookup.csv



gunzip green_tripdata_2019-09.csv.gz